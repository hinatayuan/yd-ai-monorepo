{
  "name": "yd-ai-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "build": "pnpm -r run build",
    "build:packages": "pnpm -r --filter './packages/*' run build",
    "build:app": "pnpm --filter yd-app run build",
    "dev": "pnpm --filter yd-app run dev"
  }
}
